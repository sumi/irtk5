package com.irelaxa.irtk.client;

import com.google.gwt.user.client.ui.Composite;
import com.google.gwt.user.client.ui.VerticalPanel;

public class SponsorGoalsView extends Composite {
	
public SponsorGoalsView(){
		//list of goals that are made public.
	//for the implementation of this i need a boolean field called public.
	//this will display all the goals that are public. 
		/**
         * constructor - init widget
         */
              //  initWidget(getTheWidget());
                // get the form url for the blobstore
             //   getFormUrl();
              /*  subscriptionOptions.addItem("Sponsor 10 Goals for $25");
                subscriptionOptions.addItem("Sponsor 25 Goals for $50");
                subscriptionOptions.addItem("Sponsor 50 Goals for $75");
                button.setEnabled(true); 
                */
        }



/*private VerticalPanel getTheWidget() {
    if (pWidget == null) {
            pWidget = new VerticalPanel();
            pWidget.add(getGoalSelectionPanel());
            pWidget.add(getGoalsResponse());
    }
    return pWidget;
}*/
	
}